# リユース推進部 商品管理台帳 v6.3 — セットアップ＆運用ガイド

**作成日**: 2026年5月14日　**最終更新**: 2026年5月15日（v6.3対応）  
**対象拠点**: 府中センター（リユース推進部）  
**バージョン**: v6.3（フェーズ1〜4完了 ＋ 古物営業法 第16条 完全準拠 ＋ 棚マスタ・棚先スキャン対応）  
**動作確認**: PC（Chrome）／ iPhone（Safari）にて検証済み

---

## 1. システム概要

### 構成ファイル

| ファイル | 用途 | 配置先 |
|---|---|---|
| `商品管理台帳_v6.html` | PC用メインアプリ（商品台帳・売上管理・棚卸・インポート・設定） | 各PC（Chromeで開く） |
| `scan.html` | iPhone用QRスキャン画面（商品照会・棚卸スキャン） | Netlify Drop でHTTPS配信 |
| `gas_server.gs` | Google Apps Scriptサーバー（Sheets API中継） | GASエディタに貼り付け |
| `README.md` | このドキュメント | 任意（運用マニュアル） |

### データフロー

```
┌──────────────────────┐  ┌──────────────────────┐
│ 1台目PC v6.html       │  │ 2台目PC v6.html       │
│ localStorage (PC内)   │  │ localStorage (PC内)   │
└──────────┬───────────┘  └──────────┬───────────┘
           │                          │
           └────────GAS API───────────┴───────┐
                                              │
              ┌───────────────────────────────▼──────────────┐
              │ Google Apps Script Web App                   │
              │ （シークレットキー認証）                       │
              └───────────────────┬──────────────────────────┘
                                  │
              ┌───────────────────▼──────────────────────────┐
              │ Googleスプレッドシート                        │
              │ products / inv_checked / inv_scan            │
              └───────────────────▲──────────────────────────┘
                                  │
              ┌───────────────────┴──────────────────────────┐
              │ iPhone scan.html（Netlify HTTPS）             │
              │ IndexedDB（棚卸スキャン履歴）                  │
              └───────────────────────────────────────────────┘
```

### 主な特徴

- **府中センター棚住所体系の完全対応**（v6.3） — 235番地の棚マスタ・QRラベル一括印刷
- **棚先スキャン運用**（v6.3） — 棚QR→商品QR の連続スキャンで実在棚と台帳棚の照合
- **棚番号照合レポート**（v6.2） — 台帳登録棚 vs 実際にあった棚の差分を Excel 出力
- **古物営業法 第16条 完全準拠** — 法定必須9項目を完全システム化（v6.1）
- **3年保存義務の自動管理** — 期限切れの自動検出と一括削除フロー（v6.1）
- **OAuth・Google Cloud Console 不要** — Googleアカウントだけで動作
- **オフライン耐性** — localStorage と IndexedDB に常時保存（ネット断でもデータ消失なし）
- **デュアル保存モード** — 編集2秒後に自動でクラウドへ反映
- **2台同時運用対応** — 同一スプレッドシートを複数端末から参照

### v6.1 アップデート概要（2026/5/15）

| 追加機能 | 概要 |
|---|---|
| 古物台帳の法定必須項目フル対応 | 取引区分・職業・年齢・古物の特徴の4項目を追加 |
| ¥10,000以上 / M・G部門 の自動判定 | 該当時のみ「（必須）」表示＋赤バナーで警告 |
| 商品編集画面での古物台帳編集 | 取引相手情報を後から修正可能に |
| 保存期限管理サマリーカード | 期限切れ件数・1年以内件数を一目で確認 |
| 一括削除フロー | CSV出力→確認→削除の3段階で安全に法定期限後の削除 |
| 古物台帳ビューの大幅拡張 | 13列表示・記録必須/任意バッジ・保存期限の自動計算 |

### v6.2 アップデート概要（2026/5/15）

| 追加機能 | 概要 |
|---|---|
| 棚卸タブにスキャン結果同期ボタン | GAS inv_scanシートを取得して invChecked に反映 |
| QRペイロード4項目化 | `FC-...|商品名|ランク|棚番号` で棚情報も埋め込み |
| 照合レポート 棚番号3列追加 | 台帳登録棚 / スキャン時棚 / 棚番号一致 を判定 |
| 照合レポート シート4新設 | 「棚番号不一致リスト」だけを抽出した一覧 |

### v6.3 アップデート概要（2026/5/15）

| 追加機能 | 概要 |
|---|---|
| **棚マスタタブ** | 府中センター 235番地の管理（取込・編集・印刷） |
| **棚住所Excel取込** | 採番マスターシートから一括投入（自動列マッピング） |
| **棚QRラベル一括印刷** | 1/4/8/12棚/A4 の4レイアウトから選択 |
| **棚QRフォーマット** | `SHELF|FC-S-03-01-03` で商品QRと明確に区別 |
| **棚先スキャン運用** | iPhone側で棚→商品の連続スキャンで実在棚を記録 |
| **棚未スキャン警告** | 棚QRなしで商品QRを読むと拒否（厳格運用） |

---

## 2. 初回セットアップ手順（所要15〜20分）

### 全体の流れ

| STEP | 内容 | 所要時間 |
|---|---|---|
| 1 | Googleスプレッドシート新規作成 | 1分 |
| 2 | GASエディタに gas_server.gs を貼り付け | 3分 |
| 3 | SECRET_KEY をランダム文字列に書換 | 1分 |
| 4 | Webアプリとしてデプロイ → URL取得 | 5分 |
| 5 | 商品管理台帳の「設定」タブに登録 | 2分 |
| 6 | シート初期化 → 既存データをアップロード | 3分 |

### STEP 1. Googleスプレッドシート新規作成

1. ブラウザで [https://sheets.new](https://sheets.new) を開く
2. 業務用Googleアカウントでログイン
3. ファイル名を設定（例：「府中センター 商品管理台帳DB」）

> アプリ側が起動後に `products / inv_checked / inv_scan` の3シートを自動作成します。

### STEP 2. GASエディタを開き、gas_server.gs を貼り付け

1. スプレッドシートの上部メニュー **拡張機能 → Apps Script** をクリック
2. 新しいタブでGASエディタが開く
3. 「コード.gs」の中身（`function myFunction() {...}`）を全削除
4. 同梱の `gas_server.gs` を全文コピーして貼り付け
5. プロジェクト名を変更（例：「商品管理台帳 v6 GAS API」）
6. 「保存」アイコン（フロッピー）をクリック

### STEP 3. SECRET_KEY を任意の文字列に書き換える

GASエディタ上で冒頭付近を探す：

```js
const SECRET_KEY = 'CHANGE_ME_TO_LONG_RANDOM_STRING';
```

これを任意の長いランダム文字列に書き換える（**最低20文字、英数字推奨**）：

```js
const SECRET_KEY = 'fc-ledger-7H3kPq9NmZ2VxR4tBnQ8';
```

> ⚠ このシークレットキーは後ほどHTML側にも入力します。**両方を完全一致**させることで通信を保護します。  
> メモ帳など安全な場所に控えておいてください。

保存（Ctrl+S）。

### STEP 4. Webアプリとしてデプロイ

1. GASエディタ右上の **デプロイ → 新しいデプロイ**
2. 「種類の選択」（歯車）→ **ウェブアプリ** を選択
3. 設定：
   - **次のユーザーとして実行**: ★「自分」
   - **アクセスできるユーザー**: ★「全員」  
     ※ シークレットキーで保護するため「全員」にしてOK
4. 「デプロイ」をクリック
5. 初回はアクセス許可ダイアログ：
   - 「アクセスを承認」 → アカウント選択
   - 「このアプリは Google で確認されていません」警告 → 左下「詳細」 → 「（プロジェクト名）に移動（安全ではないページ）」 → 「許可」
6. デプロイ完了画面で表示される **ウェブアプリ URL** をコピー  
   例：`https://script.google.com/macros/s/AKfycby.../exec`

### STEP 5. 商品管理台帳アプリの「設定」タブに登録

1. `商品管理台帳_v6.html` をChromeで開く
2. ナビバーの **設定** タブをクリック
3. 「接続情報」カードに：
   - **GASデプロイURL**：STEP 4 でコピーしたURL
   - **シークレットキー**：STEP 3 で設定したランダム文字列
4. 「**保存**」をクリック
5. 「**接続テスト**」をクリック → 緑のトースト「**GASへの接続に成功しました**」が出ればOK

### STEP 6. シート初期化 → 全件アップロード

1. 「データ操作」カード → **シート構成を初期化** をクリック  
   → `products / inv_checked / inv_scan` の3シートが自動作成される
2. **localStorage → クラウド（全件アップロード）** をクリック  
   → 確認ダイアログ「OK」 → ローカルの全商品がスプレッドシートに書き込まれる
3. 「処理ログ」カードで進行状況確認

---

## 3. iPhone用 scan.html のセットアップ（Netlify Drop）

scan.html は iPhone Safari でカメラを使うため **HTTPS配信が必須** です。Netlify Drop の無料プランで簡単にホスティングできます。

### 3.1 Netlify アカウント作成（初回のみ）

1. [https://app.netlify.com/signup](https://app.netlify.com/signup) を開く
2. メールアドレス＋パスワード or GitHubアカウントで登録（無料・クレジットカード不要）
3. メール認証を完了

### 3.2 scan.html をアップロード

1. ログイン後、[https://app.netlify.com/drop](https://app.netlify.com/drop) を開く
2. `scan.html` をドラッグ&ドロップ
3. 数秒でデプロイ完了 → URL が発行される（例：`https://joyful-pie-918992.netlify.app`）
4. アクセス用URL：`https://〇〇〇〇.netlify.app/scan.html`

### 3.3 iPhoneでURLを開く

URLをiPhoneに渡す方法（いずれか）：

- **メール**：自分宛にURLをメール → iPhoneで開いてリンクをタップ
- **QRコード**：PC側でURLをQR化してiPhoneで読み取る
- **手入力**：URLをiPhone Safariのアドレスバーに直接入力

### 3.4 iPhone Safariで初回設定

1. 右上の歯車アイコン（⚙）をタップ → 設定画面が下から出てくる
2. 入力：
   - **GASデプロイURL**：商品管理台帳と同じURL
   - **シークレットキー**：同じ値
   - **棚卸対象月**：当月のまま
3. 「**保存**」 → 「**接続テスト**」 → 緑のトースト「接続OK」を確認
4. 「**商品データを取得（getAll）**」 → キャッシュ件数が表示されればOK
5. 設定画面を閉じる

### 3.5 scan.html の更新方法

`scan.html` を修正した場合、Netlifyダッシュボードで再アップロード：

1. Netlifyにログイン → 該当サイトを選択
2. 「**Production deploys**」カードのドロップエリアに新しい `scan.html` をドラッグ&ドロップ
3. 10〜30秒でデプロイ完了（**URLは変わりません**）
4. iPhone Safari でページをリロード（下に引っ張って更新）

---

## 4. 2台目PCの追加手順

### 必要なもの

- 2台目PCにGoogle Chrome
- 業務用Googleアカウントが **スプレッドシートの編集者** 権限を持っていること
- 1台目で使っている GASデプロイURL と シークレットキー

### 手順

1. **ファイルコピー**：USB/共有フォルダ/メール等で `商品管理台帳_v6.html` を2台目PCに転送
2. **Chromeで開く**：ヘッダー右上のクラウド状態が「未設定」（黄）になっているはず
3. **設定タブで接続情報を入力**：1台目と同じ GAS URL/シークレットキー → 「保存」 → 「接続テスト」
4. **クラウドから取得**：「データ操作」カード → **クラウド → localStorage（全件取得）** → 全商品データがダウンロードされる
5. **デュアル保存モードON**：以後の編集を自動でクラウドへ反映
6. **商品台帳タブで件数確認**：1台目と一致していればセットアップ完了

> 既存のiPhoneは何も変更不要です。GAS URLが同じなので、どちらのPCの変更でもiPhone側に反映されます。

---

## 5. 古物営業法 第16条 準拠機能（v6.1）

### 5.1 法定必須9項目への対応

| 古物営業法 第16条 | 該当フィールド | 入力箇所 |
|---|---|---|
| ① 取引年月日 | `date` | Step 1 / 編集 |
| ② 古物の品目・数量 | `name`, `cat`, `rank` | Step 1 / Step 2 |
| ③ 古物の特徴 | `vendor.feature` 【v6.1追加】 | Step 3 / 編集 |
| ④ 取引相手の住所 | `vendor.address` | Step 3 / 編集 |
| ⑤ 取引相手の氏名 | `vendor.name`（個人）/ `company`（法人） | Step 3 / 編集 |
| ⑥ 取引相手の職業 | `vendor.occupation` 【v6.1追加】 | Step 3 / 編集 |
| ⑦ 取引相手の年齢 | `vendor.age` 【v6.1追加】 | Step 3 / 編集 |
| ⑧ 本人確認の方法 | `vendor.docType`, `docNumber` | Step 3 / 編集 |
| ⑨ 取引の区分 | `vendor.dealType`（買受/委託/交換）【v6.1追加】 | Step 3 / 編集 |

### 5.2 記録必須の自動判定ロジック

```js
function requiresVendorRecord(cost, cat) {
  const MANDATORY_CATS = ['M','G'];  // メディア・ゲーム
  return Number(cost) >= 10000 || MANDATORY_CATS.includes(cat);
}
```

- **¥10,000 以上** または **M（メディア）/ G（ゲーム）部門** に該当 → **法的記録必須**
- 該当時：注意バナーが**赤色**に切替、ラベルが「**（必須）**」表示に
- 非該当時：従来通り**黄色バナー** ＋「（任意）」表示を維持

### 5.3 3年保存義務の管理（古物営業法 第16条）

古物台帳は最終記載日から3年間の備え付け義務があります。本システムでは以下の判定で削除対象を自動検出します：

> **入荷日（date）から3年経過 かつ ステータスが「販売済」または「キャンセル」**

**在庫中は削除対象外**：最終記載日（=売却日）が未確定のため。

### 5.4 期限切れ商品の削除フロー

1. **アプリ起動時**：期限切れ件数があれば黄色トーストで通知
2. **古物台帳タブ**を開く → 上部の「**保存期限管理**」サマリーカードで件数確認
   - 期限切れ（削除対象）：X件
   - 期限まで1年以内：Y件
3. 「**期限切れ X件を一括削除**」ボタンをクリック
4. CSV ダウンロード確認ダイアログ：
   - 「OK」 → `期限超過商品_YYYYMMDD.csv` がダウンロード
   - 「キャンセル」 → CSVなしで次へ
5. 最終削除確認ダイアログで「OK」を押すと削除実行

### 5.5 古物台帳タブの 13 列表示

| 列 | 内容 |
|---|---|
| 取引日 | `date` |
| 管理番号 | `id` |
| 品名 | `name` |
| 取引区分 | `vendor.dealType`（買受/委託/交換） |
| 個/法 | `vendor.type`（個人/法人） |
| 取引相手 | `vendor.name` または `vendor.company / name` |
| 住所 | `vendor.address` |
| 職業 | `vendor.occupation` |
| 年齢 | `vendor.age` |
| 確認書類 | `vendor.docType / docNumber` |
| 古物の特徴 | `vendor.feature` |
| 記録要否 | 自動判定（必須/任意） |
| 保存期限 | 入荷日 ＋ 3年（期限切れは赤字、1年以内はオレンジ） |

### 5.6 古物台帳ビューのフィルター

- 「**記録必須のみ表示**」 — ¥10,000以上 or M/G部門 のみ表示
- 「**期限切れ・1年以内のみ表示**」 — 削除候補と期限近の商品だけに絞り込み

---

## 5.5 棚マスタと棚先スキャン運用（v6.3）

### 5.5.1 府中センター棚住所体系

採番ルール: `FC-{エリア}-{列2桁}-{行2桁}-{段2桁}`  
例: `FC-S-03-01-03` = 販売エリア・列03・行01・段03

| エリア | 説明 | 基数 | 段数 | 番地数 |
|---|---|---|---|---|
| S | 販売エリア（中央販売ゾーン） | 19基 | 5段 | 95 |
| N | 北側エリア（指令室〜金庫室） | 4基 | 5段 | 20 |
| A | 変更不可エリア（旧ジャンル棚） | 24基 | 5段 | 120 |
| **合計** | | | | **235番地** |

段03・段04 は ★ゴールデンゾーン（中段で出し入れしやすい位置）。

### 5.5.2 棚マスタの登録手順

1. v6.html → 「**棚マスタ**」タブを開く
2. 「採番マスター」シートを含む Excel をドラッグ&ドロップ
3. 「シート『📍採番マスター（全棚住所）』から 235 件の棚を読み込みました」→ **OK**
4. サマリーカードに **S=95 / N=20 / A=120 / 合計235** が表示されれば完了

> ⚠ 「全削除→取込」型のインポートです。手動で個別編集した棚は上書きされます。  
> ⚠ 棚マスタは各PCの localStorage に保存されます。複数PCで使う場合はそれぞれで取込が必要です。

### 5.5.3 棚QRラベルの印刷

1. 棚マスタタブで対象を絞り込む（エリア / ゴールデンゾーン / 検索）
2. 「絞り込み結果を一括印刷（X件）」をクリック
3. レイアウトを番号で選択（プロンプト入力）:
   - `1` → 1棚/A4（大・単独ラベル）
   - `2` → 4棚/A4（中・2×2グリッド）
   - `3` → 8棚/A4（小・2×4グリッド・**推奨**）
   - `4` → 12棚/A4（極小・3×4グリッド）
4. 印刷プレビューが別タブで開く → 「この内容で印刷する」
5. 印刷した棚ラベルを各棚板の正面に貼付

### 5.5.4 棚QRフォーマット

```
SHELF|FC-S-03-01-03
```

- 先頭が `SHELF` の棚QRと、`FC-` から始まる商品QRを scan.html が自動判別
- 棚QRは情報量が少ないシンプル形式（コードのみ）
- 棚の備考や名前は scan.html では使わず、v6.html の棚マスタから検索する設計

### 5.5.5 棚先スキャン運用フロー（iPhone scan.html）

```
[棚卸スキャンモード起動]
  ↓
[棚QRをスキャン]  ← 必ず最初にこれ
  → 上部カードが青になり「📍 現在の棚: FC-S-03-01-03」
  ↓
[その棚にある商品QRを順次スキャン]
  → scanned_shelf = 現在の棚コード で記録
  → スキャンリストに棚セパレータ付きで表示
  ↓
[次の棚へ移動]
  → 新しい棚QRをスキャン
  → トースト「棚を切替: 旧 → 新」
  → 続けて商品QRをスキャン
```

### 5.5.6 棚未スキャン時の動作（厳格運用）

棚QRを読まずに商品QRをスキャンした場合：

- 振動：警告パターン（60ms×5連打）
- 棚コンテキストカードが横揺れアニメーション
- トースト：「先に棚QRをスキャンしてください」
- **IndexedDBには記録されません**（拒否）

これにより、実物の場所を必ず記録する運用が強制されます。

---

## 6. 日常運用ガイド

### パターンA: デュアル保存モード（推奨・自動）

両PC・iPhoneすべて同一GASに接続済みの状態で、PC側でデュアル保存モードをONにする。

**作業フロー**

- 朝の作業開始時：「クラウド → localStorage（全件取得）」を一度実行（前日の他端末の変更を取り込む）
- 作業中：商品登録/編集/売上登録/棚卸チェックは普通に行うだけ → 約2秒後に自動でクラウドへ反映
- 「設定」タブの処理ログに「自動同期完了」と出れば成功

### パターンB: 手動同期（明示的・確実）

- 作業開始時：**クラウド → localStorage（全件取得）**
- 作業終了時：**localStorage → クラウド（全件アップロード）**
- どちらか1台でしか書き込まないようにすると競合ゼロ

### 競合した場合（last-write-wins）

- 同じ商品を両方のPCでほぼ同時に編集すると、**後にアップロードした方が勝ち**
- 重要な編集前にはCSV出力でローカルバックアップ推奨

### iPhone棚卸の運用フロー

1. 倉庫巡回前：scan.html を Safari で開いて「**商品データを取得**」（最新を取り込み）
2. 「**棚卸スキャン**」モード → カメラ起動 → 各商品のQRをスキャン  
   → 件数がリアルタイムでカウントされる
3. 巡回終了後：**PC側に送信** をタップ → `inv_scan` シートに一括アップロード
4. 月末：**照合レポート** → 4分類（一致/未スキャン/不明品/販売済なのにスキャン）の.xlsxがダウンロード

### 古物台帳の月次・年次運用（v6.1）

| サイクル | 推奨アクション |
|---|---|
| 毎月 | 古物台帳タブで「記録必須のみ」フィルタを当て、職業・年齢・住所など法定必須項目に未入力がないか確認 |
| 月末・年末 | 「期限切れ・1年以内のみ」フィルタで近付く削除対象を事前確認 |
| 期限到来時 | サマリーカードから一括削除（CSVバックアップを必ず取得） |

---

## 7. トラブルシューティング

### 商品管理台帳（v6.html）

| 症状 | 対処 |
|---|---|
| 接続テストで「GASから不正な応答（HTMLが返されました）」 | GASデプロイURLが間違っている / デプロイが「ウェブアプリ」でない |
| 接続テストで「Invalid secret」 | シークレットキーが HTML側 と GAS側 で食い違っている。完全一致を確認 |
| 「SECRET_KEYが設定されていません」 | gas_server.gs の SECRET_KEY を初期値から変更して再デプロイ |
| シート初期化で「You do not have permission」 | デプロイで「次のユーザーとして実行: 自分」になっていない |
| デュアル保存ONなのに反映されない | 処理ログで「自動同期エラー」を確認。GAS URL/シークレット未設定だとスキップされる |
| 別PCで取得しても件数が古い | デバウンス2秒。直前の操作からすぐ取得すると未反映の場合あり |
| Step 3 のバナーが赤くならない（v6.1） | 仕入価格が¥10,000以上 or 部門がM/Gのときのみ赤になる仕様。両方該当しなければ黄色のまま |
| 期限切れ通知が出ない（v6.1） | 在庫中の商品は対象外。販売済またはキャンセルかつ入荷日から3年経過の商品があるか確認 |
| 編集画面で古物台帳セクションが見つからない（v6.1） | 編集モードの最下部（備考の下、区切り線の後）にあります。スクロールしてご確認ください |

### iPhone（scan.html）

| 症状 | 対処 |
|---|---|
| カメラ起動で画面が真っ黒 | Safari設定 → カメラ → 「許可」を確認。他アプリのカメラ使用中なら終了 |
| カメラ権限ダイアログが出ない | scan.html を **Netlify経由のHTTPS URL** で開いているか確認（file:// では権限制限あり） |
| QRが読み取れない | 「画像から読取」で印刷物の写真を試す。読めれば検出器は正常 |
| 商品が見つからない | 設定 → 「商品データを取得」で最新化 |
| PC側に送信が失敗 | 設定タブ → 接続テスト。`inv_scan` シートが存在するか確認 |

---

## 8. GASスクリプトの更新方法（バグ修正・機能追加時）

1. GASエディタを開き、コードを編集して保存
2. 右上 **デプロイ → デプロイを管理**
3. 既存デプロイの **鉛筆（編集）** アイコン
4. バージョン：「新バージョン」
5. 「デプロイ」

> URLは変わらないので、HTML側の設定変更は不要です。  
> ⚠ 「新しいデプロイ」を作るとURLが新しくなるので、必ず「デプロイを管理」から既存を編集してください。

---

## 9. セキュリティに関する留意事項

- **GAS は「全員アクセス可能」** にしているが、**シークレットキーで実質的なアクセス制御**を行う
- シークレットキーは長くランダムなものを使用し、外部に漏らさない
- スプレッドシートの共有設定は「特定のユーザー」または社内ドメイン限定にする
- PC譲渡・廃棄時は Chrome の Site data を消去（DevTools → Application → Storage → Clear）
- **個人情報（取引相手の氏名・住所・職業・年齢）を扱う**ため、社内の個人情報保護方針に沿った取り扱いを徹底する（v6.1）

---

## 10. 内部仕様（参考）

### シート構成

| シート | 用途 |
|---|---|
| `products` | 商品データ（全件） |
| `inv_checked` | PC側棚卸チェック状態 |
| `inv_scan` | iPhoneスキャン結果 |

### `products` シート列順（v6.1）

`id, name, brand, model, cat, rank, shelf, date, cost, cond, status, channel, saleDate, salePrice, desc, vendor(JSON), createdAt, updatedAt`

### vendor オブジェクトの構造（v6.1 / JSON文字列で保存）

```javascript
{
  type:       '個人' | '法人',   // 取引相手区分
  name:       '',               // 氏名 / 代表者氏名
  company:    '',               // 会社名（法人時）
  address:    '',               // 住所
  docType:    '',               // 確認書類の種類
  docNumber:  '',               // 確認書類の番号
  occupation: '',               // 職業 【v6.1追加】
  age:        '',               // 年齢 【v6.1追加】
  dealType:   '買受' | '委託' | '交換',  // 取引区分 【v6.1追加】
  feature:    ''                // 古物の特徴 【v6.1追加】
}
```

### localStorageキー

| キー | 内容 |
|---|---|
| `fc_ledger_v3` | 商品データ |
| `fc_inv_v3` | 棚卸チェック |
| `fc_gas_url` | GAS URL/シークレット/同期モード設定 |
| `fc_shelves_v1` | 棚マスタ（v6.3 追加） |

### 棚マスタオブジェクトの構造（v6.3）

```javascript
{
  code:  'FC-S-03-01-03',  // 棚コード（主キー）
  area:  'S（販売）',        // エリア（取込時のExcel原文）
  col:   '03',              // 列番号
  row:   '01',              // 行番号
  level: '段03',            // 段（段01-段05、03・04がゴールデン）
  note:  '列03・販売ST側 ★ゴールデン',  // 備考
}
```

### IndexedDB（scan.html）

- DB名：`fc_scan_v3`
- ストア：`scans` (keyPath: scan_id)
- 列：`scan_id, scanned_id, scanned_name, scanned_rank, scanned_at, device, inventory_month, uploaded, upload_at`

### GAS API エンドポイント

| メソッド | アクション | 用途 |
|---|---|---|
| GET | `?action=ping` | 接続確認 |
| POST | `action=init` | シート構成を初期化 |
| GET | `?action=getAll` | products 全件取得 |
| POST | `action=saveAll` | products 全件上書き |
| POST | `action=addOne` | products に1件追加 |
| POST | `action=updateOne` | 管理番号で1件更新 |
| POST | `action=deleteOne` | 管理番号で1件削除 |
| GET | `?action=getInvChecked` | 棚卸チェック取得 |
| POST | `action=saveInvChecked` | 棚卸チェック上書き |
| GET | `?action=getInvScan` | スキャン結果取得 |
| POST | `action=addInvScan` | スキャン結果追加（配列OK） |

全リクエストに **シークレットキー** 必須：

- GET: クエリ `?secret=YOUR_SECRET`
- POST: bodyのJSONに `{secret: "YOUR_SECRET", action: "...", data: ...}`

### v6.1 で追加された主要関数

| 関数 | 役割 |
|---|---|
| `requiresVendorRecord(cost, cat)` | ¥10,000以上 or M/G部門 → true |
| `getExpiredProducts()` | 入荷日から3年経過 かつ 在庫中以外 の商品を返す |
| `autoCleanExpiredProducts()` | CSV出力→確認→削除のフロー全体を起動 |
| `confirmAndDelete(expired)` | 最終削除確認ダイアログを表示して削除実行 |
| `exportExpiredCsv(products)` | 期限切れ分のみを `期限超過商品_YYYYMMDD.csv` として出力 |

---

## 11. 機能一覧（v6.1 完成版）

### PC側（商品管理台帳_v6.html）

- ✅ 商品台帳（一覧・検索・フィルタリング・行クリックで詳細）
- ✅ 新規入荷登録（4ステップ右パネル）
- ✅ 商品詳細・編集・売上登録・QRコード生成印刷
- ✅ 売上管理（月間目標進捗・販路別売上）
- ✅ 売上分析（概要・販路別・部門別・予算達成 の4タブ）
- ✅ 棚卸（チェックリスト・月次Excel出力）
- ✅ 古物台帳（取引記録一覧・13列表示・2フィルター）
- ✅ CSV出力
- ✅ Excelインポート（列マッピングUI・置き換え/追加モード）
- ✅ Google Sheets連携（GAS方式・デュアル保存）
- ✅ **古物営業法 第16条 完全準拠（v6.1）**
  - 取引区分・職業・年齢・古物の特徴のフィールド追加
  - ¥10,000以上 / M・G部門の自動判定で必須/任意切替
  - 商品編集画面での古物台帳情報の編集
- ✅ **保存期限自動管理（v6.1）**
  - 期限切れ件数のサマリー表示
  - CSV出力→確認の安全な一括削除フロー
  - 起動時の期限切れ警告トースト
- ✅ **スキャン結果同期機能（v6.2）**
  - 棚卸タブの「スキャン結果を同期」ボタン
  - GAS inv_scanシート → invChecked への自動反映
- ✅ **棚マスタ管理（v6.3）**
  - 235番地の登録・編集・削除
  - Excel取込（採番マスターシート自動検出）
  - 棚QRラベル一括印刷（4レイアウト）

### iPhone側（scan.html）

- ✅ 商品照会モード（QR → 商品詳細表示）
- ✅ 棚卸スキャンモード（QR → IndexedDB蓄積 → PC送信）
- ✅ 照合レポート出力（4分類・3シート構成のxlsx）
- ✅ QRコード検出（BarcodeDetector + ZXing + jsQR の3段階フォールバック）
- ✅ オフライン対応（IndexedDB）
- ✅ 画像読取（フォトライブラリから）
- ✅ 管理番号手入力フォールバック
- ✅ **棚先スキャン運用（v6.3）**
  - 棚QR検出と現在の棚コンテキスト管理
  - 棚切替セパレータ表示
  - 棚未スキャン時の厳格警告（商品スキャン拒否）

---

## 12. ファイル配置まとめ

| ファイル | 推奨配置先 |
|---|---|
| `商品管理台帳_v6.html` | 各PCの `C:\Users\事務局３\kobutu_daichou\` 等 |
| `scan.html` | Netlify Drop（HTTPS配信） |
| `gas_server.gs` | Google Apps Script（クラウド） |
| `README.md` | 任意（運用マニュアル） |

---

*以上 — 2026年5月15日（v6.3 棚マスタ・棚先スキャン対応版）／ 動作確認済み*
